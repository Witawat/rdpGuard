# วิธีใช้งาน (Usage)

## Web UI

เปิดเบราว์เซอร์ → `http://127.0.0.1:8123` (พอร์ตเปลี่ยนได้ใน [CONFIG.md](CONFIG.md))

ครั้งแรกต้องล็อกอินด้วยรหัสผ่าน — หาได้จาก:

```bat
python run.py password
```

> ตอนรันเป็น service ต้องใช้คำสั่งนี้จากสิทธิ์ admin ด้วย (config อยู่ใต้ %ProgramData%)

### หน้าแรก (Dashboard)

| การ์ด | ความหมาย |
|---|---|
| ล็อกอินล้มเหลว (24 ชม.) | จำนวน Event 4625 ทั้งหมดใน 24 ชั่วโมงล่าสุด |
| ล็อกอินสำเร็จ (24 ชม.) | จำนวน Event 4624 ที่นับได้ |
| IP กำลังถูกบล็อก | จำนวน IP ที่มี rule อยู่ในตาราง blocked ตอนนี้ |
| บล็อกทั้งหมด | ผลรวมตลอดกาล (ประวัติ + กำลังบล็อก) |
| กฎที่ตั้ง | สรุปค่าการตรวจจับปัจจุบัน (max ครั้ง / กรอบเวลา / ชั่วโมงบล็อก) |

แถบหัว (header) แสดงสถานะ: **กำลังเฝ้าระวัง** (เขียว) / **เฝ้าระวังปิดอยู่** (เหลือง) / **ไม่ทำงาน** (แดง) + บริบทการรัน (Windows Service / standalone)

หน้า refresh อัตโนมัติทุก 3–5 วินาที

### การตรวจจับ (พาเนลควบคุม)

ส่วน "การตรวจจับ" (ใต้การ์ดสถิติ): **สวิตช์เปิด/ปิดเฝ้าระวังทั้งหมด** + **chip เปิด/ปิดแต่ละ engine** (RDP/OpenSSH/MSSQL/IIS/MySQL/Generic) — กดแล้วมีผลทันที ไม่ต้อง restart

### สถานะระบบ (พาเนลตรวจสอบ)

ส่วน "สถานะระบบ (ส่วนประกอบสำคัญ)" บอกว่าของที่ต้องใช้ทำงานได้จริงหรือไม่:

| ส่วนประกอบ | สถานะที่เป็นไปได้ |
|---|---|
| Security event log (แหล่งหลัก) | ✔ อ่านได้ / ✘ อ่านไม่ได้ (สิทธิ์ไม่พอ — service/SYSTEM อ่านได้) |
| Windows Firewall (ตัวบล็อก) | ✔ เข้าถึง + มีสิทธิ์เพิ่ม rule / ○ เข้าถึงได้แต่ไม่มีสิทธิ์ / ✘ เข้า COM ไม่ได้ |
| สิทธิ์โปรแกรม | ✔ admin/SYSTEM / ✘ ผู้ใช้ธรรมดา (บล็อกไม่ได้) |
| Monitor / เฝ้าระวัง | ✔ รันอยู่ / ✘ ไม่ได้รัน |
| แต่ละ Engine | ✔ แหล่งข้อมูลพร้อม / ✘ อ่านไม่ได้ / ○ ยังไม่พบแหล่ง / ⊘ ปิดอยู่ |

ปุ่ม **"ทดสอบบล็อกจริง (firewall)"** — เพิ่ม + ลบ rule ทดสอบ (IP TEST-NET ไม่กระทบใคร) แล้วรายงานผลว่าบล็อกได้จริงหรือไม่ และถ้าไม่ได้บอกเหตุผลให้ (เช่น "ต้องรันด้วย admin")

### Windows Service (พาเนลควบคุม)

ส่วน "Windows Service" ในหน้าแรก (ใต้การ์ดสถิติ) แสดงสถานะ + ปุ่มควบคุม:

| ปุ่ม | การทำงาน |
|---|---|
| ติดตั้ง | ลงทะเบียนเป็น Windows Service (เริ่มอัตโนมัติตอน boot) |
| เริ่ม / หยุด / รีสตาร์ท | ควบคุม service |
| ถอน | ถอนการติดตั้ง service |

- ปุ่มจะ **ปิดใช้งานอัตโนมัติ** ตามบริบท: รันใน service เอง / ไม่มีสิทธิ์ admin / สถานะไม่ตรง (เช่น กด "เริ่ม" ตอนรันอยู่แล้ว) — พร้อมข้อความอธิบายเหตุผล
- ต้องเปิดโปรแกรมด้วย **Run as administrator** ถึงจะใช้ปุ่มได้
- รันใน service อยู่ → ควบคุมผ่าน services.msc หรือ CLI (`rdpguard.exe stop` ฯลฯ) เท่านั้น

### เหตุการณ์ล่าสุด

ตารางแสดง failed/success logon ล่าสุด 80 รายการ: เวลา (UTC), IP, ผู้ใช้, โดเมน, LogonType, **แหล่ง** (RDP/OpenSSH/MSSQL/IIS Web/MySQL — รู้ว่าโจมตีผ่านช่องทางไหน), ประเภท (ล้มเหลว/สำเร็จ/NTLM)

### IP ที่ถูกบล็อก / Whitelist / Blacklist

- ตารางมีคอลัมน์ **"ประเทศ"** แสดงธง + ชื่อประเทศของแต่ละ IP (GeoIP ออนไลน์ + cache — ถ้าเครื่องออฟไลน์แสดง "-" ไม่กระทบการบล็อกแต่อย่างใด)
- ปุ่ม **ปลดบล็อก** — ถอด rule firewall + ลบออกจากตารางทันที
- กล่อง **บล็อกด้วยมือ** — ใส่ IP (หรือ CIDR เช่น `203.0.113.0/24`) + จำนวนชั่วโมง (0 = ถาวร) แล้วกดปุ่ม

### Log การทำงาน

พาเนลด้านล่างสุด แสดง `rdpguard.log` ล่าสุด 250 บรรทัด (refresh อัตโนมัติทุก 5 วินาที + ปุ่ม "โหลดใหม่") — ใช้ดูว่า engine ทำงานปกติ/เจอเหตุการณ์อะไรบ้าง

### Whitelist / Blacklist

| รายการ | พฤติกรรม |
|---|---|
| Whitelist | IP/CIDR ที่จะไม่มีวันถูกบล็อกอัตโนมัติ (เช่น IP สำนักงาน, IP ตัวเอง) |
| Blacklist | IP/CIDR ที่จะถูกบล็อกทันทีแม้พยายามครั้งแรก |

### ตั้งค่า

เปลี่ยนค่าการเฝ้าระวัง/ตรวจจับ/firewall/web UI ได้จากหน้าเว็บ กด **บันทึกการตั้งค่า** แล้วมีผลทันที (service โหลด config ใหม่เอง โดยไม่ต้อง restart)

## CLI

ทุกคำสั่งใช้ผ่าน `python run.py <คำสั่ง>` หรือ `python -m rdpguard <คำสั่ง>` (รันจากโฟลเดอร์โปรเจกต์) ถ้าสร้าง exe แล้วใช้ `rdpguard.exe <คำสั่ง>`

> **รันโดยไม่มีคำสั่ง** (เช่น ดับเบิลคลิก `rdpguard.exe`) = เริ่มเฝ้าระวังทันที (monitor + web UI) + เปิดเบราว์เซอร์ให้อัตโนมัติ และถ้าเป็น**ครั้งแรก** จะเจอ **Setup Wizard** ตั้งค่าทีละขั้นก่อน

| คำสั่ง | ความหมาย | ต้อง admin? |
|---|---|---|
| `install` | ติดตั้ง Windows Service (เริ่มอัตโนมัติตอน boot) | ใช่ |
| `remove` | ถอนการติดตั้ง service | ใช่ |
| `start` / `stop` / `restart` | ควบคุม service | ใช่ |
| `status` | ดูสถานะ service (installed? running?) | ไม่ |
| `run` | รันแบบ foreground (monitor + web) — ใช้ทดสอบ/ดู log สด | แนะนำใช่ (จะบล็อก IP ได้) |
| `web` | รันเฉพาะ Web UI โดยไม่เฝ้าระวัง — ใช้ทดสอบ UI | ไม่ |

### Setup Wizard (ครั้งแรก)

ตอนรันครั้งแรก (ยังไม่เคยตั้งค่า) Web UI จะแสดงวิซซาร์ด 4 ขั้นตอนให้ตั้งค่าก่อนใช้งาน:

1. **ยินดีต้อนรับ** — อธิบายการทำงาน + แนะนำการติดตั้งเป็น service
2. **การตรวจจับ** — จำนวนครั้ง/กรอบเวลา/ระยะเวลาบล็อก/ข้าม LAN
3. **Web UI** — host/พอร์ต/รหัสผ่าน
4. **พร้อมใช้งาน** — สรุปการตั้งค่า → กด "เสร็จสิ้น" เพื่อบันทึกและเริ่มเฝ้าระวังทันที

กด "ข้ามการตั้งค่า" ได้ถ้าไม่อยากตั้ง (จะใช้ค่าเริ่มต้นทั้งหมด) — ตั้งค่าทีหลังได้ที่หน้า "ตั้งค่า" ใน dashboard
| `block <ip> [ชั่วโมง]` | บล็อก IP ด้วยมือ (ค่าเริ่มต้น 24 ชม., 0 = ถาวร) | ใช่ |
| `unblock <ip>` | ปลดบล็อก IP | ใช่ |
| `password` | แสดงรหัสผ่าน Web UI | ไม่ |
| `password reset` | สุ่มรหัสผ่าน Web UI ใหม่ | ไม่ |
| `version` | แสดงเวอร์ชัน | ไม่ |
| `help` | แสดงคำสั่งทั้งหมด | ไม่ |

### ตัวอย่าง

```bat
:: ดูสถานะ
python run.py status

:: บล็อก IP ด้วยมือ 12 ชั่วโมง
python run.py block 203.0.113.9 12

:: บล็อกถาวร
python run.py block 203.0.113.9 0

:: ปลดบล็อก
python run.py unblock 203.0.113.9

:: รีเซ็ตรหัสผ่าน Web UI
python run.py password reset
```

## ไฟล์ข้อมูล

ตำแหน่งขึ้นกับโหมดการรัน:

| โหมด | ตำแหน่ง (config.ini, rdpguard.db, rdpguard.log) |
|---|---|
| exe (`rdpguard.exe`) | โฟลเดอร์เดียวกับ exe |
| source (`python run.py`) | `%ProgramData%\RDPGuard\` (ถ้าเขียนไม่ได้ → `C:\Users\<user>\.rdpguard\`) |

| ไฟล์ | ความหมาย |
|---|---|
| config.ini | ตั้งค่าทั้งหมด (แก้ผ่าน Web UI ได้) |
| rdpguard.db | SQLite: เหตุการณ์, blocked, whitelist, blacklist |
| rdpguard.log | log การทำงาน |

> โหมด exe: ถ้าข้าง exe ยังไม่มีข้อมูล แต่มีข้อมูลเก่าที่ `%ProgramData%\RDPGuard\` อยู่
> (จากโหมด source/เวอร์ชันก่อนหน้า) ระบบจะคัดลอก config.ini + rdpguard.db มาไว้ข้าง exe อัตโนมัติตอนรันครั้งแรก

## ทดสอบว่าระบบทำงาน

1. เปิด RDP ไปที่เครื่องตัวเอง (จากเครื่องอื่น) แล้วลองใส่รหัสผิด 5+ ครั้งภายใน 10 นาที
2. เปิด Web UI → ควรเห็นเหตุการณ์ล้มเหลวสะสม และ IP นั้นโดนบล็อกอัตโนมัติ (การ์ด "IP กำลังถูกบล็อก" เพิ่มขึ้น)
3. ลองรัน `python run.py status` + `wf.msc` → ดู rule ชื่อ `RDPGuard Block <IP>`
4. ปลดบล็อกได้จากปุ่มใน Web UI หรือ `python run.py unblock <IP>`
