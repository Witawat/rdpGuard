# Design (Web UI)

Mood: "ผู้ดูแลเซิร์ฟเวอร์เปิดหน้าเว็บดูสถานะความปลอดภัยตอนเช้า — สว่าง สะอาด อ่านครั้งเดียวเข้าใจ มี shield สีน้ำเงินจุดเดียว"

## Palette (OKLCH)

| Token | Value | ใช้กับ |
|---|---|---|
| `--bg` | `oklch(0.97 0.005 250)` | พื้นหลังหน้า (ฟ้าจาง ๆ สบายตา) |
| `--surface` | `oklch(1 0 0)` | การ์ด/พาเนล/ตาราง |
| `--surface-2` | `oklch(0.955 0.01 250)` | แถว hover / input |
| `--border` | `oklch(0.90 0.012 250)` | เส้นขอบ |
| `--ink` | `oklch(0.25 0.03 250)` | ตัวหนังสือหลัก |
| `--ink-2` | `oklch(0.42 0.035 250)` | ตัวหนังสือรอง/หัวกลุ่มตั้งค่า |
| `--muted` | `oklch(0.55 0.03 250)` | ป้ายกำกับ/ตัวช่วย (ผ่าน AA 4.5) |
| `--accent` | `oklch(0.55 0.16 255)` | ปุ่มหลัก (น้ำเงิน — โทน security) |
| `--accent-ink` | `oklch(1 0 0)` | ตัวหนังสือบนปุ่มน้ำเงิน |
| `--accent-soft` | `oklch(0.94 0.03 255)` | พื้นหลัง highlight / badge |
| `--ok` | `oklch(0.52 0.12 155)` | สถานะปกติ |
| `--ok-soft` | `oklch(0.955 0.04 155)` | พื้นหลังสถานะปกติ |
| `--warn` | `oklch(0.56 0.12 75)` | คำเตือน |
| `--warn-soft` | `oklch(0.96 0.05 80)` | พื้นหลังเตือน |
| `--danger` | `oklch(0.52 0.19 25)` | error / ถูกบล็อก |
| `--danger-soft` | `oklch(0.955 0.05 25)` | พื้นหลัง error |

- สถานะทุกชนิดมีทั้ง **จุดสี + คำกำกับภาษาไทย** เสมอ ไม่พึ่งสีอย่างเดียว (accessibility)
- สีความหมาย: เขียว = ทำงานปกติ, เหลือง = ปิดอยู่/คำเตือน, แดง = ผิดปกติ/ถูกบล็อก

## Typography

- UI: system stack `Segoe UI, system-ui, sans-serif`
- Mono (IP / เวลา / ค่าทางเทคนิค): `Cascadia Code, Consolas, monospace`
- ขนาด: 12 (ตารางหัว/ช่วย) / 13 (ตัวหลัก) / 15 (หัวพาเนล) / 17 (logo) / 26 (ตัวเลขสถิติ)

## Layout

- **แถบหัว (sticky)**: logo "RDPGuard" + pill สถานะ (กำลังเฝ้าระวัง/ปิด/ไม่ทำงาน) + pill บริบท (Windows Service / standalone) + pill เวอร์ชัน + ปุ่มออกจากระบบ
- **การ์ดสถิติ 5 ใบ**: ล้มเหลว 24 ชม. / สำเร็จ 24 ชม. / IP กำลังถูกบล็อก / บล็อกทั้งหมด / สรุปกฎ — ตัวเลข mono ขนาดใหญ่
- **แนวโน้ม 7 วัน**: แท่งสีแดง/เขียวสรุป Events ล้มเหลวและสำเร็จรายวันโดยไม่ใช้ library ภายนอก
- **พาเนล "เหตุการณ์ล่าสุด"**: ตาราง (เวลา/IP/ผู้ใช้/โดเมน/LogonType/ประเภท) scroll ในพาเนล 420px, badge สีแยก fail/success/ntlm
- **พาเนล "IP ที่ถูกบล็อก"**: แถวเพิ่ม IP + ตารางพร้อมปุ่มปลดบล็อก (อันตราย = ปุ่มแดง)
- **Whitelist / Blacklist**: สองพาเนลคู่กัน
- **ประวัติและ Audit Log**: ตารางแยกสำหรับดูการปลดบล็อกและการกระทำของผู้ดูแล พร้อมส่งออก CSV
- **ตั้งค่า**: ฟอร์มกลุ่มตาม section (Log/ฐานข้อมูล/เฝ้าระวัง/ตรวจจับ/firewall/web UI/engine เพิ่มเติม/แจ้งเตือน) ปุ่มบันทึก 1 จุด พร้อม secret masking และสถานะค่าที่ยังไม่บันทึก
- **Log การทำงาน**: แสดง path/ขนาดไฟล์, เลือกไฟล์หมุนเวียน/จำนวนบรรทัด 250/500/1000, ค้นหา, หยุด refresh และดาวน์โหลด
- **Session / Remote**: แสดง session ปัจจุบัน refresh ทุก 10 วินาที; ใช้ `qwinsta`/`query session` และ fallback WTS API โดยไม่เรียก PowerShell
- **Toast**: ด้านล่างขวา fixed (ผลบันทึก/error) เข้าด้วย fade + translateY 8px
- **Login**: กล่องกลางจอ 340px พร้อมข้อความ error แดง

## หลักการ

- ไม่มีกราฟแบบ library หนักหรือ gradient — แนวโน้มใช้แท่ง CSS ขนาดเล็ก ส่วนข้อมูลหลักแสดงเป็นตาราง/ตัวเลขตรง ๆ
- ทุกแถว hover มีพื้นหลังจาง (surface-2)
- motion เฉพาะ transition ≤120ms + toast; `prefers-reduced-motion: reduce` → ทุกอย่าง instant
- z-index: header 10 / toast 40
- ปุ่ม: primary = พื้นน้ำเงิน+ตัวขาว / secondary = พื้นขาว+ขอบ / ปุ่มอันตราย = ตัวแดง
- responsive: การ์ดเป็น grid auto-fit, สองคอลัมน์ (whitelist/blacklist) ย่อเป็น 1 คอลัมน์เมื่อจอแคบ; กลุ่มตั้งค่าจะเรียงเป็นคอลัมน์เดียวเมื่อจอกว้างน้อยกว่า 980px
